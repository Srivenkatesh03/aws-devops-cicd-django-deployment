apiVersion: v1
kind: ConfigMap
metadata:
  name: sms-config
  namespace: sms
data:
  DB_ENGINE: django.db.backends.postgresql
  DB_HOST: sms-postgres
  DB_PORT: "5432"
  DEBUG: "False"
  ALLOWED_HOSTS: "*"